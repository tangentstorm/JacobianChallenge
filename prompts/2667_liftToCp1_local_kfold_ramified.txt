Please rename this session to "2667. Prove liftToCp1_local_kfold_ramified".

Target file: Jacobian/HolomorphicForms/MeromorphicToCp1.lean
Target sorry: theorem liftToCp1_local_kfold_ramified
Allowed writes: prefer Jacobian/HolomorphicForms/MeromorphicToCp1.lean and local mapping/order API files.
Forbidden files: Jacobian/Challenge.lean, Jacobian/WorkPackets/StatementBank.lean

Task: prove local k-fold ramification for the CP¹ lift of a meromorphic function.

Current blocker:
`MeromorphicFunctionType` currently supplies local meromorphicity of the raw map, but the target theorem needs the project `HasLocalKfoldRamification` local-mapping package for `meromorphicToCp1 X f`.

Expected approach:
1. Split finite and infinity target-chart cases.
2. Reuse `AnalyticLocalMapping.analytic_local_mapping_theorem` through `local_kfold_ramified_of_contMDiff` where possible.
3. Prove compatibility between `mapAnalyticOrderAt (meromorphicToCp1 X f)` and the meromorphic local normal form in finite/infinity charts.

Anti-cheat:
- Do not produce arbitrary finite sets of preimages.
- Do not change `mapAnalyticOrderAt` or `meromorphicToCp1` to make the theorem trivial.

Verification:
```sh
lake build Jacobian.HolomorphicForms.MeromorphicToCp1
python3 scripts/fix-sorries.py
python3 scripts/audit-sorries.py sorries.jsonl
```
