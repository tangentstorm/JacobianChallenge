Please rename this session to "2666. Prove meromorphicMapToSphere_of_inverse_order_one_frontier".

Target file: Jacobian/HolomorphicForms/HarmonicFunctions.lean
Target sorry: theorem meromorphicMapToSphere_of_inverse_order_one_frontier
Allowed writes: prefer Jacobian/HolomorphicForms/HarmonicFunctions.lean and order/divisor API files.
Forbidden files: Jacobian/Challenge.lean, Jacobian/WorkPackets/StatementBank.lean

Task: convert a holomorphic complex-valued function whose inverse has order-one zero at `P` into a `MeromorphicMapToSphere` with pole divisor `Divisor.point P`.

Expected approach:
1. Read `Meromorphic.lean`, `VanishingOrder.lean`, and any divisor/order bridge files.
2. Build the OnePoint extension of `F`, prove the finite-locus meromorphic/continuity fields from `hholo`.
3. Prove the pole divisor is exactly `Divisor.point P` from `horder`.

Anti-cheat:
- Do not create a `MeromorphicMapToSphere` by assigning `poleDivisor := Divisor.point P` unless all structure fields are proved from `F`, `hholo`, and `horder`.
- If the divisor API cannot express "inverse order one implies simple pole", add a named honest lemma for that bridge and report it if it remains open.

Verification:
```sh
lake build Jacobian.HolomorphicForms.HarmonicFunctions
python3 scripts/fix-sorries.py
python3 scripts/audit-sorries.py sorries.jsonl
```
