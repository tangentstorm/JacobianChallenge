Please rename this session to "2673. Prove polygon4g_boundary_arcs_are_loops".

Target file: Jacobian/Periods/CellularChainComplex.lean
Target sorry: theorem polygon4g_boundary_arcs_are_loops
Allowed writes: Jacobian/Periods/CellularChainComplex.lean and narrowly scoped helper lemmas in Jacobian/Periods/Polygon4g.lean if needed.
Forbidden files: Jacobian/Challenge.lean, Jacobian/WorkPackets/StatementBank.lean

Task: prove that every boundary arc endpoint of the standard `4g`-gon maps to the single quotient vertex.

Expected approach:
1. Prove endpoint equalities for adjacent arcs:
   `boundaryParam g k 1 = boundaryParam g (k+1) 0`, modulo periodicity.
2. Use side-pairing generators `Polygon4g.mk_a_pair` and `Polygon4g.mk_b_pair` at `t = 0` and `t = 1` to identify vertices across each handle block.
3. Chain these equivalences to show all `mk g (boundaryParam g k 0/1)` equal `mk g (boundaryParam g 0 0)`.
4. Handle `g = 0` separately using direct equality of `boundaryParam 0 k t`.

Anti-cheat:
- Do not change `boundaryParam` or `Polygon4g.SideRel`.
- The proof should use quotient equality via `SideRel` or existing `mk_*_pair` lemmas, not assume all disk endpoints are definitionally equal.

Verification:
```sh
lake build Jacobian.Periods.CellularChainComplex
python3 scripts/fix-sorries.py
python3 scripts/audit-sorries.py sorries.jsonl
```
