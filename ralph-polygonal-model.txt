# ralph task list — polygonal-model setup
#
# One task per non-comment, non-empty, non-DONE line.
# Order = priority (top picked first).
#
# Reference plan: ref/plans/polygonal-model.md
# Reference Lean state: Jacobian/Periods/Polygon4g.lean (164 LOC, sorry-free)
#                       Jacobian/Blueprint/Sec03/PolygonalModel.lean (real signature, body sorry)
#
# Strategy (per ref/plans/polygonal-model.md): Stage B (analytic↔topological
# genus bridge) is mostly independent of Stage A (surface classification) and
# is achievable today against Mathlib v4.28.0. Start there.
#
# ---------- Stage B: analytic ↔ topological genus bridge ----------

DONE: Build Stage B sub-leaf B1: define a smooth real 2-manifold structure on a complex 1-manifold X. Add a new file Jacobian/Periods/SmoothRealStructure.lean with `theorem ChartedSpaceComplex_to_smoothReal2 : [ChartedSpace ℂ X] [IsManifold (modelWithCornersSelf ℂ ℂ) ⊤ X] → [ChartedSpace (EuclideanSpace ℝ (Fin 2)) X] ∧ [IsManifold (modelWithCornersSelf ℝ (EuclideanSpace ℝ (Fin 2))) ⊤ X]`. Use Complex.equivRealProd from Mathlib to compose chart maps. Sorry-bound body OK. ~80 LOC.

DONE: Build Stage B sub-leaf B3: define `topologicalGenus` for a compact connected oriented 2-manifold via singular homology rank. Add a new file Jacobian/Periods/TopologicalGenus.lean with `noncomputable def topologicalGenus (M : Type*) [TopologicalSpace M] [CompactSpace M] [ConnectedSpace M] : ℕ := (Module.finrank ℤ (H_1 M ℤ)) / 2` using Mathlib.AlgebraicTopology.SingularHomology.Basic. Sorry-bound or `noncomputable` placeholder OK if H_1 finrank machinery is partial. ~50 LOC.

DONE: Sketch a project-side `Orientable` typeclass: add `Jacobian/Periods/Orientable.lean` with `class Orientable (M : Type*) [TopologicalSpace M] : Prop where existsConsistentChartChoice : True := trivial` as a placeholder that future work will fill in. Document the typeclass in the file's docstring as the analytic-orientability hook for compact 2-manifolds. ~30 LOC.

# ---------- Stage A: surface classification (the big one) ----------
# Skipped from this ralph batch — these need their own focused planning round.
# See ref/plans/polygonal-model.md §4 Stage A.
