# ralph task list — polygonal-model Stage A surface classification
#
# Reference plan: ref/plans/surface-classification.md
# Parent plan: ref/plans/polygonal-model.md
#
# Order = priority (top picked first). Seeds chosen for low risk +
# unblock-effect per §6 of the surface-classification plan.

# DONE (Aristotle 6dc419db): A2.1 — EdgeWord inductive type + normal form predicate + wordQuotient. Shipped 2026-05-02 in Jacobian/Periods/EdgeWord.lean (77 LOC, 1 placeholder sorry on sidePairingRel).

# A1.1: finite chart cover of compact 2-manifold. Add Jacobian/Periods/CompactChartCover.lean with `theorem compact_2manifold_has_finite_chart_cover : [ChartedSpace (EuclideanSpace ℝ (Fin 2)) M] [CompactSpace M] : ∃ (n : ℕ) (cover : Fin n → OpenPartialHomeomorph M (EuclideanSpace ℝ (Fin 2))), (∀ i, cover i ∈ atlas (EuclideanSpace ℝ (Fin 2)) M) ∧ ⋃ i, (cover i).source = univ`. Pure Mathlib finite-subcover. ~50 LOC. See ref/plans/surface-classification.md §4 A1.1.

# A3.1: polygon4g_eq_standard_word_quotient — show our project-side Polygon4g g and the wordQuotient of the standard 4g-gon word are the same quotient. Add to Jacobian/Periods/EdgeWord.lean (after A2.1 lands). ~30 LOC. See ref/plans/surface-classification.md §4 A3.1.

# A2.3: tietze_swap_lemma — single-step Tietze move on EdgeWord (e.g. `xy y⁻¹x → ε`) as a `Quotient.sound` proof in the wordQuotient. Add to Jacobian/Periods/EdgeWord.lean. ~30 LOC. See ref/plans/surface-classification.md §4 A2.3.

# ---------- bigger leaves (need A2.1 + A1.1 first) ----------
# Skipped from this ralph batch — see ref/plans/surface-classification.md §4 A1.4, A2.4, A2.5.
