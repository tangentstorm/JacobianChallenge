# ralph task list — polygonal-model Stage A surface classification
#
# Reference plan: ref/plans/surface-classification.md
# Parent plan: ref/plans/polygonal-model.md
#
# Order = priority (top picked first). Seeds chosen for low risk +
# unblock-effect per §6 of the surface-classification plan.

# ---------- in flight at Aristotle (do not duplicate via ralph) ----------
# DONE (Aristotle 6dc419db): A2.1 — EdgeWord inductive type + normal form predicate + wordQuotient. Shipped 2026-05-02 in Jacobian/Periods/EdgeWord.lean (77 LOC, 1 placeholder sorry on sidePairingRel).
# IN_PROGRESS (Aristotle 6407236f): A1.1 — compact_2manifold_has_finite_chart_cover.
# IN_PROGRESS (Aristotle 28888261): A2.3 (list-level) — InverseCancel relation on EdgeWord + WordEq via Relation.ReflTransGen.

# ---------- active ralph seed ----------

# DONE (Claude direct, 2026-05-02): Replace the placeholder `sidePairingRel` with a real `DiskC → DiskC → Prop` relation. EdgeWord.lean is now sorry-free; for the standard word, `sidePairingRel g (standardWord g) = Polygon4g.SideRel g` (proven). Non-standard words use `Eq` as a placeholder until the general construction lands per A2.2.

# ---------- A3.1 (now unblocked) ----------
# A3.1 (`polygon4g_eq_standard_word_quotient`) is now attackable — `wordQuotient g (standardWord g)` is the quotient of `DiskC` by `Polygon4g.SideRel g`, which is the same structure as `Polygon4g g`. The proof should be a `Quotient.congrRight`-style argument plus the `sidePairingRel_standardWord` lemma now in EdgeWord.lean.

# ---------- bigger leaves ----------
# Skipped from this ralph batch — see ref/plans/surface-classification.md §4 A1.4, A2.4, A2.5.
