# ralph task list — polygonal-model Stage A surface classification
#
# Reference plan: ref/plans/surface-classification.md
# Parent plan: ref/plans/polygonal-model.md
#
# Order = priority (top picked first). Seeds chosen for low risk +
# unblock-effect per §6 of the surface-classification plan.

# ---------- in flight at Aristotle (do not duplicate via ralph) ----------
# DONE (Aristotle 6dc419db): A2.1 — EdgeWord inductive type + normal form predicate + wordQuotient. Shipped 2026-05-02 in Jacobian/Periods/EdgeWord.lean (77 LOC, 1 placeholder sorry on sidePairingRel).
# IN_PROGRESS (Aristotle 6407236f): A1.1 — compact_2manifold_has_finite_chart_cover.
# IN_PROGRESS (Aristotle 28888261): A2.3 (list-level) — InverseCancel relation on EdgeWord + WordEq via Relation.ReflTransGen.

# ---------- active ralph seed ----------

Replace the placeholder `sidePairingRel` in Jacobian/Periods/EdgeWord.lean with a real disk-side relation. Currently `sidePairingRel : ℕ → EdgeWord _ → Unit → Unit → Prop := sorry`. Replace with a definition over the closed unit disk `Metric.closedBall (0 : ℂ) 1` that pairs boundary points on opposite sides of the 4g-gon parametrisation, in the standard side-pairing pattern dictated by the EdgeWord. Reference Jacobian/Periods/Polygon4g.lean (the existing 4g-gon side-pairing relation `SideRel`) for the pattern. The new `sidePairingRel` should generalise `SideRel` to arbitrary edge words: `SideRel` is the special case where the word is `standardWord g`. Update `wordQuotient` accordingly so it quotients the closed unit disk by the new relation.

# ---------- A3.1 (blocked) ----------
# A3.1 (`polygon4g_eq_standard_word_quotient`) is blocked until the above seed lands — it requires `wordQuotient g (standardWord g)` to actually be the quotient of the disk by `SideRel`, not the placeholder Quot of a Unit relation.

# ---------- bigger leaves ----------
# Skipped from this ralph batch — see ref/plans/surface-classification.md §4 A1.4, A2.4, A2.5.
